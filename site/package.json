{
  "name": "bite-sized-biographies",
  "version": "0.1.0",
  "private": true,
  "description": "Hugo site for Bite-Size Bios.",
  "scripts": {
    "build": "hugo --gc --minify --cleanDestinationDir",
    "dev": "hugo server -D --bind 127.0.0.1"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.16",
    "@tailwindcss/typography": "^0.5.19",
    "tailwindcss": "^4.1.16",
    "pagefind": "^1.4.0"
  }
}
